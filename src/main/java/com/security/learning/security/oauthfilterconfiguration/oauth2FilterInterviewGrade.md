## OAuth2 real game
```text
- The real game is the google console configuration, and to get the yaml configuration.
    - Consent screen.
    - Internal, external some buzz word were there.
    - Client id, scope, some secret key will be given to by the google console, that requires to be handled
        only and only in the backend, that's why backend engineering is so reliable.
    - And yet, that callback url, i need to recheck it over again.
- Rest is pretty easy, well to be honest, easier than jwt.

Question
- Has the user deletion that we countered in the jwt been omitted?
- Is it the best option for api requests?
- Gotta learn the architecture again?
- What is OAuth2User used?
- How can we do, login, logout option, if login option is to be given over to google authentication
server, at-least shouldn't the user have log out option here?

```